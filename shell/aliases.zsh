# Generic aliases
alias ll='ls -lah'
alias la='ls -A'
alias l='ls -CF'

# Make helpers
alias mcr='make compile-reqs'
alias mr='make run'
alias ms='make shell'
alias ml='make lint'
alias mt='make test'

# Git helpers
alias gcm='git checkout main && git pull origin main'
